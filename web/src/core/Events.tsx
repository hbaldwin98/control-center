import { EmptyState, Page, PageHeader } from "@cc/ui";

/** Events. Filled in at milestone 2. */
export function Events() {
  return (
    <Page>
      <PageHeader title="Events" lede="The live event log, filterable by dot-segment pattern." />
      <EmptyState>Arrives with milestone 2.</EmptyState>
    </Page>
  );
}
