import { EmptyState, Page, PageHeader } from "@cc/ui";

/** Costs. Filled in at milestone 5. */
export function Costs() {
  return (
    <Page>
      <PageHeader title="Costs" lede="Spend by plugin, then job, then logical model, over time." />
      <EmptyState>Arrives with milestone 5.</EmptyState>
    </Page>
  );
}
