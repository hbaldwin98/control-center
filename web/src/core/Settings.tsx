import { EmptyState, Page, PageHeader } from "@cc/ui";

/** Settings. Filled in at milestone 5. */
export function Settings() {
  return (
    <Page>
      <PageHeader title="Settings" lede="Credentials, effective model routes, notification rules, and channels." />
      <EmptyState>Arrives with milestone 5.</EmptyState>
    </Page>
  );
}
