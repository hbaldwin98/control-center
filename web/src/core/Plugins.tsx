import { EmptyState, Page, PageHeader } from "@cc/ui";

/** Plugins. Filled in at milestone 3. */
export function Plugins() {
  return (
    <Page>
      <PageHeader title="Plugins" lede="Enable and disable plugins, set budgets, and see health and the last error." />
      <EmptyState>Arrives with milestone 3.</EmptyState>
    </Page>
  );
}
